# CLAUDE.md 예시 — 세션 기록과 함께 쓰기

이 훅은 대화를 파일로 남기기만 한다. 규칙이 없어도 동작하므로 `CLAUDE.md` 에 적을 것은
**기본 동작과 다르게 하고 싶은 것**뿐이다.

## 기록에서 빼기

기록하고 싶지 않은 세션에는 `#nolog` 를 **단독 줄**로 친다. 그 세션은 파일이 만들어지지 않고,
`nolog_purge.py` 가 원본 트랜스크립트도 함께 지운다.

붙여넣은 실행 로그는 `#로그` 마커 뒤에 코드펜스로 두면 기록에서 빠진다.

```
#로그
```
ERROR 2026-09-18 ... 아주 긴 스택트레이스
```
```

## 이어서 할 일은 어디에 남기나

이 훅은 할 일 목록을 만들지 않는다. 진행 중인 작업·보류 사유처럼 다음 세션이 알아야 할 것은
Claude Code 의 메모리(`project` 타입)에 남고 세션 시작 시 자동으로 주입된다.

세션 자체를 이어서 하려면 `claude -r <세션ID>` 를 쓰고, 지난 대화를 읽히려면
`~/day1/claude_log/` 의 해당 파일 경로를 전달한다.
